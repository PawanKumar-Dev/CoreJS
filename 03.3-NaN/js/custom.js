// What is "NaN" ?

// Stands for "Not a Number"
// NaN is a property of the global object.
// It shows when we perform a number operation on a non-number value

let a = "hello";      // Non number value or string as we know

console.log(a/4);     // We are performing a number operation on a string. It return "NaN"