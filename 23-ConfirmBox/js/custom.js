// A confirm box is used if you want user to verify something.

// When confirm box pops up, user can click either "OK" or "Cancel" to proceed. If "OK", it returns true. If "Cancel", then returns false.
// Always returns a boolean value

confirmValue = confirm("Are you sure?");

console.log(confirmValue);
console.log(typeof(confirmValue));