// trim() -- is essiantial methods when validation data in JS. It remove whitespaces before and after a string.
let text = "       Hello World!        ";
console.log(text.trim());

// ==============================================
// split() -- it splits a string into an array
// It takes the argument as--- on what basis you want to break the string?
let str = "Hi, how are you doing today?";

// Splitting on basis of white spaces
console.log(str.split(" "));
console.log(str.split(","));
